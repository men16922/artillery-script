config:
  target: "http://34.64.94.35"
  phases:
    - duration: 20
      arrivalRate: 2
      name: Warm up
scenarios:
  # We define one scenario:
  - name: "just get hash"
    flow:
      - get:
          url: "/hash/123"